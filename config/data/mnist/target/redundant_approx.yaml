# @package _global_

# labels_count_main.target is a placeholder: it isn't used when
# acquisition.epig_classification.target_class_dist is defined.

acquisition:
  epig_classification:
    target_class_dist: [0.5, 0.5, 0]

data:
  label_counts_main:
    target:
      1: 1_000
      7: 1_000